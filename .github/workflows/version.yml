name: Inject build version
# Disabled — version is now bumped manually before each push
on:
  workflow_dispatch:

jobs:
  version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo "Manual version bump workflow — run locally instead"
