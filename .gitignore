# Local dev server files — not needed for the hosted app
server.js
server.py
start.command
wristlog-data.json
.DS_Store
node_modules/

# Xcode user-specific files
xcuserdata/
*.xcuserstate

# Supabase CLI temp
supabase/.temp/

# Dev credentials (never commit)
dev-config.js

# Claude Code project instructions (contains tokens)
CLAUDE.md

# Apple auth keys (sensitive)
*.p8

# Test coverage output
coverage/

# Playwright artifacts
test-results/
.last-run.json

# Large local assets — not needed for the hosted app
competitors/
e2e/fixtures/
app store files/

# Local tooling / editor config
.superpowers/
.claude/settings.json

# Local research scratch. Note the rest of docs/research/ IS tracked, so this
# is a single-file entry rather than ignoring the directory.
docs/research/2026-06-30-social-platform-feature-catalog.md

# Claude Code local settings (may contain approved command strings)
.claude/settings.local.json

# Generated hero-image previews (wrotate_hero_images.py writes them)
scripts/hero-images/preview/

# Before/after screenshots from design-system passes — local reference only
docs/design/design-system/

# Pages build output (scripts/build-site.sh)
_site/

# local dev server allowlist (scripts/dev-site.mjs)
.devsite/
