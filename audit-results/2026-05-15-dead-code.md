# Dead Code Audit — WRotate (May 15, 2026)

**File:** index.html (22,068 lines)
**Total removable:** ~574 lines (2.6% of file)

---

## Large Functions (never called)

| Function | Line | Size | Why dead |
|----------|------|------|----------|
| `toggleTgListen()` | 20269 | 124 lines | Old measurement system, replaced by `toggleMsrListen()` |
| `fetchWishlistDetails()` | 19479 | 140 lines | Button `wl-fetch-btn` removed from HTML, no callers |
| `checkBatchWatchValues()` | 18012 | 92 lines | Never called anywhere |
| `fetchWishlistImage()` | 19623 | 42 lines | Never called anywhere |
| `openAf2DebugPreview()` | 17978 | 24 lines | Debug preview, never called |
| `openModalCropAdjuster()` | 15032 | 21 lines | Watch modal crop, never called |
| `openWlModalCropAdjuster()` | 19679 | 18 lines | Wishlist crop, never called |
| `scrollToPost()` | 19888 | 18 lines | Never called anywhere |
| `initLandingCarousel()` | 21861 | 16 lines | `#landing-carousel` element doesn't exist |
| `quickLog()` | 15143 | 14 lines | Never called anywhere |
| `toggleWatchPresent()` | 21280 | 11 lines | Never called anywhere |
| `deleteLogFromStats()` | 16476 | 8 lines | Never called anywhere |
| `clearWlModalImg()` | 19714 | 7 lines | Never called anywhere |
| `clearWatchModalImage()` | 15068 | 6 lines | Never called anywhere |
| `setCollView()` | 14404 | 6 lines | Never called anywhere |
| `classifyDevice()` | 11904 | 6 lines | Logic moved to server-side `admin_traffic_stats` RPC. Still exported in `wrotate_test.js` for tests — remove from both. |
| `clearNpWatch()` | 9978 | 4 lines | Never called anywhere |

---

## Welcome Modal System (~235 lines)

Entire system is dead — `maybeShowWelcome()` has a hard `return;` on line 10480.

**HTML (lines 2320-2447, ~128 lines):**
- `#welcome-modal` — 6-step onboarding wizard with profile setup, watch add, discover
- Dot indicators (`#wd-1` through `#wd-6`)

**CSS (lines 1898-1899, ~2 lines):**
- `#welcome-modal` background override
- `.welcome-step.hidden` display rule

**JS (lines 10480-10586, ~107 lines):**
- `maybeShowWelcome()` — disabled with `return;`
- `pauseWelcome()` — pauses wizard to open another modal
- `resumeWelcome()` — resumes after pause
- `welcomeStep()` — step navigation
- `closeWelcome()` — close handler
- `saveWelcomeProfile()` — save profile from wizard
- `_welcomePendingStep` variable

**Note:** `acceptEula()` at line 10547 references `welcome-modal` to hide it — update that line when removing.

---

## Feature Flag Infrastructure (~18 lines)

`FEATURE_FLAGS` object is empty (line 4580). All flags were shipped and checks removed.

- `FEATURE_FLAGS = {}` (line 4580-4581)
- `featureFlag(name)` (line 4582-4586) — reads localStorage `ff_` keys
- `setFeatureFlag(name, val)` (line 4587-4589) — writes localStorage
- `renderDevFlags()` (line 11376-11393) — renders empty admin dev flags panel
- `<div id="admin-dev-flags">` (line 2843) — empty container

**Keep if:** planning to use feature flags again soon. Otherwise remove.

---

## Notes

- `stopTgListen()` (line 20394) is called as a safety guard from 3 places (lines 12187, 12229, 15011). These check `_tgListening` which is always false since `toggleTgListen` is never called. Safe no-ops but leave them until `toggleTgListen` removal is confirmed stable.
- `isBase64()` (line 16773) — never called in index.html but exported in `wrotate_test.js` and tested. Remove from index.html, keep in test helper.
- `tg-bph-select` HTML element IS still used by other code — do not remove it with `toggleTgListen`.
