# WRotate Audit — 2026-07-19 (Consolidated Summary)

**Scope:** ~92-101 commits since the 2026-06-29 audit (8,778 insertions / 81 files).
New surface: the Resend→SES email migration (7 commits), `brands` RLS lockdown,
`admin_email_engagement` rewrite, Stats wear leaderboard + `isWearEntry` wear rule,
wishlist brand folders, campaign grouping, feed measurement-card fix.

**Method:** five parallel audits (security, reliability, performance, usability, bugs).
Every finding required evidence — live DB queries, `EXPLAIN ANALYZE`, deployed-endpoint
probes, or screenshots inspected visually. Unverifiable claims are labelled as such.

Category reports: `2026-07-19-{security,reliability,performance,usability,bugs}-audit.md`.

---

## Top items (ranked)

| # | Sev | Cat | Finding | Location | Status |
|---|-----|-----|---------|----------|--------|
| 1 | **High** | Sec | Six `SECURITY DEFINER` `admin_*` RPCs are executable by **anon** — no guard, no REVOKE. **Verified exploitable with the public anon key.** Leaks business metrics and a per-user-UUID → activity-volume map. | live DB grants | OPEN |
| 2 | **High** | Rel | `DAILY_EMAIL_LIMIT = 100` is a stale **Resend** constant. Broadcast queue stalled: **324 pending**, ~26/night, 0 on busy nights. | `send-broadcast/lib.ts:195` | OPEN |
| 3 | **High** | Rel | SES client has **no retry**; `drainQueue` stamps transient failures permanent `failed`, and the drain only selects `pending` — those recipients are dropped forever. | `_shared/ses.ts:31`, `send-broadcast/index.ts:412` | OPEN |
| 4 | **High** | Bug | SES webhook stamps every `opened`/`clicked` with `mail.timestamp` (the **send** time), not the event time. Verified in prod. | `ses-webhook/lib.ts:46` | OPEN |
| 5 | **High** | Bug | `renderDowReport()` is called with no argument and reads raw `logs` — ignores **both** the period filter and the measurement rule. | `index.html:18963` | OPEN |
| 6 | **High** | Bug | Year in Review + Monthly Review count measurement shares as wears. 4 of 5 measurement logs are the only log that date → genuine phantom wears. | `index.html:18461,18569` | OPEN |
| 7 | **High** | UX | Leaderboard watch names truncate on every phone — 4 of 6 cut off at 390px. Defeats the card's purpose. | `index.html:18998` | OPEN |
| 8 | **High** | UX | Leaderboard uncapped: 30 watches → **1,496px card**, 20 identical `#11 · 0 wears —` rows above every other Stats section. | `index.html:18986` | OPEN |
| 9 | Med | Sec | Missed third instance of the brand-escaping bug: Add-from-Photo edit row still inlines the brand into `onmousedown` with `escHtml` (doesn't escape `'`). | `index.html:21157` | OPEN |
| 10 | Med | Sec | `auto-add-brand` never re-validates Claude's `canonical_name` before writing it with `is_canonical:true` → prompt-injection → globally-visible poisoned row. Chains with #9 into stored XSS. | `auto-add-brand/index.ts:105,121,137` | OPEN |
| 11 | Med | Sec | `auto-add-brand` is unauthenticated and unrate-limited (verified: 200 with no auth header). Each call burns an Anthropic web-search request. | `auto-add-brand/index.ts:27` | OPEN |
| 12 | Med | Bug/Rel | Admin open/click rates count raw events, not distinct emails → **200%, 300%** open rates rendered. | `sql/2026-07-19-admin-email-engagement-internal-filter.sql:46` | OPEN |
| 13 | Med | Perf | Analytics scripts pull a **42 MB** corpus daily (was ~2 MB) — 14.5 s/run, 90% network. **The obvious fix is wrong** — see below. | `scripts/rollout-check.py:93`, `weekly-measurement-review.py:231` | OPEN |
| 14 | Med | Perf | `admin_email_engagement` aggregates `email_events` all-time, no date bound, `SELECT *`. Intake jumped **4.6×** in the SES week. | live RPC | OPEN |
| 15 | Med | Perf | `send-broadcast` makes **one GoTrue call per recipient** (400-1,000 sequential). | `send-broadcast/index.ts:204` | OPEN |
| 16 | Med | Rel | `drainQueue` sends-then-marks with no lease → crash mid-wave or concurrent drain re-sends. | `send-broadcast/index.ts:377` | OPEN |
| 17 | Med | Rel | `ses-webhook` not idempotent; `email_events` has no unique constraint. **5 duplicate `sent` rows already exist.** | `ses-webhook/index.ts:125` | OPEN |
| 18 | Med | Bug | Watch recommendation compares unique wear-days against a raw-row baseline — different units, ~7.5% inflated "neglected" scores. | `index.html:18707` | OPEN |
| 19 | Med | UX | The `%` is unlabeled and **disagrees between Stats (period) and Track (all-time)** with no explanation. Same watch: `57%` vs `58%`. | `index.html:19001,15850` | OPEN |
| 20 | Med | UX | New `.wl-view-btn` toggles measure **27×28px** — the primary control for brand folders. Never added to the M6 40px rule. | `index.html:1338,1630` | OPEN |
| 21 | Med | UX | Leaderboard rows look tappable (identical to Track's) but are inert — no `onclick`, `role`, `tabindex`. | `index.html:18994` | OPEN |
| 22 | Med | UX | Add-from-Photo hierarchy: fixed in empty states, **still inverted in the toolbars** — the surface users see every visit. | `index.html:2914` vs `4056` | CARRIED |
| 23 | Med | Rel | Client `logs`/`brands` reads unpaginated. Cap not currently hit (max 422 logs/user) but truncation would silently re-order the leaderboard. | `index.html:6693,6695` | OPEN |
| 24 | Med | Rel | `run-campaign` tracking-upsert failure only logged → `backfill_daily` cohorts re-emailed daily until it succeeds. | `run-campaign/index.ts:132` | OPEN |
| 25 | Med | Rel | `weekly-review` LaunchAgent has `RunAtLoad=false` and no same-week guard; no dead-man's switch. | plist | CARRIED |

Plus 10 Low findings across the category reports (constant-time compare, error-detail
disclosure, brand-request tap target + modal title, folders-view row mixing, duplicate
`MOST WORN` tile, leaderboard period not shown in-card, `loadMyProfile` null deref,
image viewer index after thumbnail swap, brand-rebuild sentinel, `.btn-sm` 34px).

---

## The trap worth reading before anyone optimises #13

The natural fix for the 42 MB analytics pull is "filter to `session_summary` rows".
**That would silently break both scripts.** Measured:

```
is_summary=false | 27,689 rows | containing 'psBE=' = 16,713
is_summary=true  |  2,163 rows | containing 'psBE=' =      0
```

`psBE=` is the sole 2.0 detection signature both the rollout tracker and the Sunday
review key on. It appears in **zero** summary rows. A summary-only filter makes both
report **0 users on 2.0** — a wrong answer, not a crash. The fix must be an incremental
cursor over `created_at`, not a row-type filter.

---

## Self-assessment: most of the new findings are from this cycle's own work

Of the 25 ranked items, **11 originate in changes made on 2026-07-19**:

- #5, #6 — I introduced `isWearEntry` as "the single definition of a wear" and routed
  four call sites through it, but missed three more on the same page (`renderDowReport`,
  Year in Review, Monthly Review). I then shipped Help copy asserting *"Every section on
  Stats follows the period filter. Measurement shares aren't wears"* — **which is false
  for those three sections.** The claim was written without enumerating consumers.
- #7, #8, #19, #21 — the Most Worn card shipped without being viewed on a phone with a
  realistic collection. 30 watches produces a 1,496px wall of zero rows.
- #9, #10 — I fixed brand-name escaping at two of three sites and did not grep for the
  third; and `is_canonical:true` (my change) is what promotes an unvalidated
  Claude-supplied name into every user's autocomplete.
- #12, #14 — both in the `admin_email_engagement` RPC I rewrote today.
- #20 — the 27×28px toggles are the result of today's "fit the toolbar on one line" fix,
  which trimmed horizontal padding on a control whose height was already sub-target.

**Pattern:** the failures cluster where a change was verified against the *specific*
reported symptom rather than against the *class* of problem — one call site instead of
all consumers, one screen width instead of a realistic data volume, two escaping sites
instead of a grep.

---

## Confirmed FIXED since 2026-06-29

| Item | Evidence |
|---|---|
| M-1/M-2 `search_path` pins on `admin_user_detail`, `admin_last_active` | Absent from the live unpinned list — the re-pin held |
| P1 admin modal ~1.1 s tick-log scan | `idx_ttl_summary_user` present; client double-LIKE gone |
| P3 / #7 `send-broadcast` unpaginated reads | `fetchAllRows` applied to all four reads, ported to `run-campaign` |
| R29-1 `weekly-measurement-review.py` traceback email | Present; deployed copy `diff`-clean |
| R29-2 `fetch_paginated` silent truncation | Fixed |
| UX29-1 Add-from-Photo CTA (empty states) | Genuinely fixed — but see #22 for the toolbars |

## Verified clean (checked, not assumed)

- **SNS webhook signature is genuinely cryptographic** — cert host pinned to
  `^sns\.[a-z0-9-]+\.amazonaws\.com$`, verification precedes the `SubscribeURL` fetch.
  An attacker **cannot forge `email_events` rows** without an AWS-signed payload.
- **No secrets logged, returned, or echoed** across all seven migrated senders.
- **`brands` RLS enforcing** — one SELECT-only policy, service-role-only writer.
- **`loadMyProfile` hardening is sound** — `PGRST116` cleanly separated, `insert` not `upsert`.
- **Batch partial-failure handling is good** — one bad recipient cannot abort a batch;
  `results[i]` reliably maps to `msgs[i]`.
- **The leaderboard's core logic is correct** — O(logs + watches·log watches), `_dataGen`
  memo intact, competition ranking, `pct: null` vs `0`, DST-safe `periodCutoff`.
- **Dark mode clean**; no horizontal scroll at 375/390px on Stats, Track, Wishlist.
- **`campaignGroupOf`** checked against 60 days of real subjects — no collisions.

## Not assessed

- Feed measurement-card rendering for multi-image posts — could not be reproduced through
  the mock harness. Code read as sound; **no verdict either way**.
- Whether the SES account is out of sandbox (could not query the SES API). If still
  sandboxed, #2's correct limit is 200 rather than the real quota — stall halves but remains.

## Suggested order

1. **#1** — one migration: `REVOKE` + guard on six functions (also closes their missing `search_path` pins).
2. **#2** — one constant; unblocks 324 queued recipients tonight.
3. **#5, #6 + Help correction** — the shipped Help text is currently false.
4. **#3, #4** — SES retry semantics and event timestamps.
5. **#7, #8** — one commit on the Most Worn card layout.
6. **#9, #10, #11** — `auto-add-brand` hardening + the third escaping site.
7. **#20** — one-line CSS.
</content>
</invoke>
