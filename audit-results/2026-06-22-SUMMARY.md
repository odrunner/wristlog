# WRotate Audit — 2026-06-22 (Consolidated Summary)

Deep-dive bug hunt + four-category audit (security / reliability / usability / performance).
Run autonomously. Previous full cycle: 2026-06-12. Window since: 10 days, ~10 commits
(admin email metrics, follow/private-profile gating, campaign grouping, new multi-drip campaign UI).

Per-category detail in:
- `2026-06-22-bugs.md`
- `2026-06-22-security-audit.md`
- `2026-06-22-reliability-audit.md`
- `2026-06-22-usability-audit.md`
- `2026-06-22-performance-audit.md`

No code was modified — read-only investigation. Top items below were traced end-to-end; the
two Critical/High auth items were re-verified by reading the source directly.

## Headline: the email/webhook surface is the weak spot this cycle

The recent admin-email + campaign work added an attack/abuse surface that isn't fully locked down.
Fix these first:

| # | Sev | Finding | Location | Status |
|---|-----|---------|----------|--------|
| 1 | **CRITICAL** | `resend-webhook` loads `RESEND_WEBHOOK_SECRET` but never verifies it — any anonymous POST writes attacker-controlled rows into `email_events`, poisoning every admin engagement metric. Function is `--no-verify-jwt`. | `resend-webhook/index.ts:5,26` | VERIFIED |
| 2 | **HIGH** | `run-campaign` has **no auth** — `Deno.serve(async (_req) => …)` ignores the request. Anyone hitting the URL forces the drip-campaign batch to fire and burns Resend quota. | `run-campaign/index.ts:37` | VERIFIED (NEW fn) |
| 3 | **HIGH** | Two active drip campaigns with the same `delay_days` both email the same cohort the same day — new multi-drip UI shipped with no cross-campaign dedup; run-campaign dedups only per `campaign_id`. New drips default to `delay_days:30`. | `run-campaign/index.ts:94-98` | NEW |
| 4 | **HIGH** | `feedback-to-github` has no webhook secret and injects raw user `details` into an issue whose `auto-bug` label triggers automated codegen — prompt-injection into auto-fix. | `feedback-to-github/lib.ts:61,65` | NEW angle |
| 5 | **HIGH** | SSRF in `search-watch-image` — authed user, `redirect:"follow"`, no private-IP block → internal/metadata hosts reachable. | `search-watch-image` | CARRIED, re-rated up |
| 6 | **HIGH** | `acceptFollowRequest` raw `.insert` bails on `23505` and never deletes the pending request → stuck Accept/Decline buttons forever (re-creates the contradictory state the recent follow fixes targeted). | `index.html:8613` | NEW bug |
| 7 | **HIGH** | `send-broadcast` cohort tracking uses `.insert` not `.upsert` — one duplicate fails the whole 100-row batch, so that cohort gets re-emailed next run. | `send-broadcast/index.ts:275` | NEW bug |

## Fixes implemented 2026-06-22 (code complete + tested; deploy status noted)

All Critical/High code-defect items fixed in the working tree. Verification: full
unit suite (1146) ✅, mocked E2E (108) ✅, Deno function tests (117, incl. new
SSRF + Svix-signature tests) ✅. SW cache bumped v786→v787.

| # | Item | Code fix | Deploy status |
|---|------|----------|---------------|
| 1 | resend-webhook signature | Verifies Svix `svix-id/timestamp/signature` over the raw body; 5-min replay window. Proven against the official Svix test vector. | ✅ **DEPLOYED + ENFORCING 2026-06-22.** `RESEND_WEBHOOK_SECRET` set; unsigned POST now returns 401 (verified). Watch-item: confirm the next *real* Resend event still lands in `email_events` (validates the secret string matches Resend exactly). |
| 2 | run-campaign auth | Requires `x-campaign-secret` OR admin JWT. | ✅ **DEPLOYED + ENFORCING 2026-06-22.** `CAMPAIGN_TRIGGER_SECRET` set + cron updated to send the header; no-auth and wrong-secret calls both return 401 (verified). Cron sends the matching secret, so the daily run + admin button still pass. |
| 3 | run-campaign cross-campaign dedup | `emailedThisRun` Set ensures a user gets at most one campaign per run. | ✅ **DEPLOYED 2026-06-22** (active now — no secret needed). |
| 4 | acceptFollowRequest stuck request | Treats `23505` as success so the pending request is still deleted. | ✅ **SHIPPED 2026-06-22** (pushed to main → frontend). |
| 5 | send-broadcast idempotency | Cohort tracking `.insert`→`.upsert(ignoreDuplicates)`. | ✅ **DEPLOYED 2026-06-22**. |
| 6 | feedback-to-github injection | Uses the trusted DB row (not request body) + sanitizes/fences user text against prompt-injection into auto-codegen. | ✅ **DEPLOYED 2026-06-22**. |
| 7 | search-watch-image SSRF | `isSafeFetchUrl` blocks non-http(s), creds, odd ports, metadata IP, localhost, private/loopback/link-local IPv4+IPv6, internal hostnames. | ✅ **DEPLOYED 2026-06-22** (smoke test 200). Residual: DNS-rebinding not caught. |

**#8 onboarding (`maybeShowWelcome` // DISABLED)** intentionally NOT changed — re-enabling a deliberately disabled feature is a product decision, not a bug fix.

### Deploy runbook (when you're back)
1. **resend-webhook:** Resend dashboard → Webhooks → copy the Signing Secret (`whsec_…`) → `npx supabase secrets set RESEND_WEBHOOK_SECRET=whsec_…` → `npx supabase functions deploy resend-webhook --no-verify-jwt` → confirm `email_events` keep arriving.
2. **run-campaign:** `npx supabase secrets set CAMPAIGN_TRIGGER_SECRET=$(openssl rand -hex 24)` → update the `run-email-campaigns` cron to send header `x-campaign-secret: <that value>` → `npx supabase functions deploy run-campaign --no-verify-jwt` → confirm a no-auth call returns 401.
3. **send-broadcast / feedback-to-github / search-watch-image:** `npx supabase functions deploy <name> --no-verify-jwt` each, then `npm run test:smoke`.
4. **Frontend (#4):** `git push origin main`.

## Follow-up batch 2026-06-22 — quick wins + reliability hardening (8 items, all FIXED + deployed)

Verified: 1146 unit · 108 mocked E2E · 103 Deno function tests (incl. new
`effectivePrefs` + `isKnownSegment` tests) all green. SW cache v787→v788.

- **#22 FIXED** — measure complete-msg no longer overwrites the yellow low-confidence retry warnings (removed the trailing `var(--text)` override). index.html.
- **#23 FIXED** — troubleshoot tips now fire on the 2.0 `duration_cap` stop reason. index.html:24162.
- **#24 FIXED** — admin "Run now" / "Send to cohort" use `showConfirm()` modal, not native `confirm()`. index.html.
- **#25 FIXED** — measure-duration copy reconciled to "30–60s (up to 90)" matching the 90s cap. index.html:2391,3034.
- **#9 FIXED** — send-broadcast rejects unknown/typo'd segments (`isKnownSegment`), so a typo can't silently blast all opted-in users. Deployed.
- **#13 FIXED** — run-campaign aborts on `internal_accounts` read error (won't email internal accounts) and skips a campaign on send-history read error (won't re-send to all). Deployed.
- **#15 FIXED** — send-email per-key prefs merge (`effectivePrefs`): a prefs object missing a newer key now uses that category's default instead of silently skipping. Deployed.
- **#16 FIXED** — report-notify returns 502 on Resend failure instead of falsely reporting `sent:true`. Deployed.

Commit `9f469b2`. Remaining backlog after this batch: Medium #10–#12, #14, #17–#21 and the Low items below (plus the deliberately-untouched onboarding #8).

## By category (counts)

- **Bugs:** 0 Crit · 3 High · 4 Med · 5 Low
- **Security:** 1 Crit · 3 High · 6 Med · 6 Low
- **Reliability:** 1 High new + 1 High carried · 4 Med new + 6 Med carried · several Low
- **Usability:** 2 High · 13 Med · ~13 Low · 1 NEW · 1 FIXED (light cycle)
- **Performance:** 0 new defects · 5 High / 12 Med / 8 Low all carried-forward (status verification)

## Quick wins (high value / low effort)

- Add signature verification to `resend-webhook` (secret already loaded — just use it). [Crit #1]
- Add a shared-secret / auth check to `run-campaign` and `feedback-to-github`. [High #2,#4]
- `send-broadcast`: `.insert` → `.upsert({ignoreDuplicates:true})` (match run-campaign). [High #7]
- `acceptFollowRequest`: treat `23505` as success, then delete the request (match `followUser`). [High #6]
- Pin `search_path` on 4 SECURITY DEFINER admin RPCs (`admin_user_detail`, `admin_last_active`, `admin_active_days`, `admin_traffic_stats`). [Med, carried]
- Usability 1-minute fixes still un-applied from June 12: remove `completeMsg.style.color` override (24324); add `'duration_cap'` to troubleshoot-tips (24159); replace 2 native `confirm()` in admin campaign flow (14719,14762) with existing `showConfirm()`.
- Fix duration copy mismatch — UI says "20-45s"/"30-60s" but cap is 90s (2391, 3034, 23473).

## Standing backlog worth scheduling (carried)

- **`timegrapher_tick_logs` is unbounded** — 45 MB → 60 MB (+33%) in 10 days, no pruning job. 3 s debug inserts fire for all native users. Both an admin-modal LIKE-scan perf risk and a cost risk. (perf N3, N4)
- **`rollout-check.py` re-downloads the cumulative `messages` corpus daily** from a fixed date against the growing 60 MB table. (perf N2)
- Broadcast `segment` not whitelisted — a typo'd segment silently broadcasts to all opted-in users. (reliability N6)
- Deep Test / piezo runners not exempt from auto-stop/share gates; no cross-runner mutex. (reliability N3/N4/N5)
- Onboarding still hard-disabled — `maybeShowWelcome()` returns `// DISABLED`. (usability U-ONB-001, High)
- `.git` is 806 MB / 9,798 loose objects / 0 packs — `git gc` never run. (perf N11)
