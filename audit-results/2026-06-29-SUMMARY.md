# WRotate Audit — 2026-06-29 (Consolidated Summary)

Four-category audit (security / reliability / usability / performance), run with one agent per category. Previous full cycle: 2026-06-22 (+ a 2026-06-28 UX-consistency pass). Window since: ~7 days, ~195 commits — a large UX-consistency refactor plus new functional surfaces: wishlist **Add-from-Photo**, **badge-post inline folding**, **edit-post occasion/strap**, **onboarding "Measure accuracy" completion**, **measure weak-signal graceful stop**, the **weekly measurement-review engine**, and the **admin user-detail modal** (created_at fix shipped 2026-06-29).

Per-category detail:
- `2026-06-29-security-audit.md`
- `2026-06-29-reliability-audit.md`
- `2026-06-29-usability-audit.md`
- `2026-06-29-performance-audit.md`

Read-only investigation; findings classified NEW vs CARRIED, with carried-item status verified against current code + live DB.

## Top items (ranked)

| # | Sev | Cat | Finding | Location | Status |
|---|-----|-----|---------|----------|--------|
| 1 | **High** | Rel | `weekly-measurement-review.py` does **not** email its traceback on failure (only prints to stderr) — a Sunday-review crash silently stalls the one-change-per-week loop with no alert. Siblings call `notify_failure(tb)`. | `scripts/weekly-measurement-review.py:254-258` | OPEN |
| 2 | **High** | Perf | Admin user-detail modal does ~1.1 s of redundant `timegrapher_tick_logs` seq-scanning per open: client double-LIKE (960 ms, no time bound) + the RPC's `last_active` summary scan (134 ms, predates today's change). | index.html:13760-13763; RPC `v_last_active` | OPEN (carried N4, compounded) |
| 3 | **Med** | Rel | `fetch_paginated` reintroduces the NEW-5 silent-truncation bug (`if isinstance(page, dict): break` returns partial rows as complete). Skews which weekly fix ships. | `scripts/weekly-measurement-review.py:58-66` | OPEN (NEW regression) |
| 4 | **Med** | Sec | `admin_user_detail` lost its `search_path` pin (my 2026-06-29 migration used `CREATE OR REPLACE` without re-declaring `SET search_path`). Live `proconfig: null`. | `sql/2026-06-29-admin-user-detail-watch-created-at.sql:5` | OPEN (NEW regression) |
| 5 | **Med** | Sec | `admin_last_active` lost its `search_path` pin (same pattern, 2026-06-28 migration). | `sql/2026-06-28-last-active-engagement.sql:21` | OPEN (NEW regression) |
| 6 | **Med** | UX | Add-from-Photo CTA hierarchy inverted: `btn-primary` on Collection empty state, `btn-ghost` on Wishlist. May be intentional (commit c045f6b). | index.html:16555 vs 21454-21457 | OPEN |
| 7 | Med-low | Perf | `send-broadcast` recipient reads unpaginated — silent PostgREST 1000-row cap → correctness risk past 1000 users. | `send-broadcast` | OPEN (carried) |

## Good news this cycle

- **`timegrapher_tick_logs` shrank 60 MB → 20 MB** (48,352 → 16,582 rows) — archival ran this window. The whole carried perf cluster now runs against a 3× smaller table. *(Caveat: no recurring pg_cron prune job found in repo — confirm archival recurs so it doesn't regrow; TODO.md Stage 2 is PAUSED.)*
- **No new Critical or High security findings.** All 06-22 Critical/High fixes still enforcing (resend-webhook 401 on unsigned, run-campaign auth, SSRF block, atomic rate limits, constant-time HMAC).
- New functional surfaces are largely well-built: **Edit-Post** (double-submit guard + optimistic rollback + orphan-file cleanup), **badge folding** (race-safe drain-before-await), **onboarding** (idempotent), **both Add-from-Photo paths** (timeouts, 429 backoff, graceful partial state, no XSS/SSRF).
- Carried items confirmed FIXED: measure `completeMsg` color override (U-TG-V2-001), `duration_cap` troubleshoot tips (U-TG-V2-002), native confirm()=0 (U-CONFIRM-001), tick-log flush retry/`_flushTickLog` (NEW-4), send-broadcast segment whitelist (N6), nightly/rollout fail-loud (NEW-5 — but reintroduced in weekly-review, item #3).

## Still-open carried items (lower priority)

- **RM3 (Rel, Low):** `persistMsrReading`/`saveMsrReading`/`shareMsrFromComplete` don't disable the Save button during the awaited insert → fast double-tap inserts two `timegrapher_results` rows. (index.html:25550+)
- **NEW-9 (Rel, Low):** global handlers toast raw "Something went wrong" / "BG fail: <stack>" with no dedup.
- **R29-4 (Rel, Low):** nightly-analysis `sys.exit(1)` isn't caught by its Exception-only handler, so that path doesn't email.
- **UX29-2 (UX, Low):** `weak_signal`/`no_ticks_signal_lost` stops don't escalate to the richer troubleshoot panel.
- **R29-3 (Rel, Low):** weak-signal stop reveals the Save section for an 11–24-dot reading the engine called too weak.
- **P2 (Perf, Med, carried):** rollout-check/weekly-review pull the cumulative `messages` corpus unfiltered by row type (lower magnitude now — ~2 MB summaries).
- **P5 (Perf, Low):** `piezo_raw_captures` = 28 MB / 127 rows but dormant (admin-gated, default-off, last activity June 6-7) — reclaimable dead storage.
- **Sec Low:** test-account password hardcoded in scripts; `extract-url-meta` admin-only redirect SSRF.

## Quick wins (high value / low effort)

1. **[#4/#5 Sec]** Re-pin search_path: `ALTER FUNCTION public.admin_user_detail(uuid) SET search_path = pg_catalog, public;` and same for `admin_last_active()`. One statement each. *(I caused #4 today.)*
2. **[#1 Rel]** Add `notify_failure(tb)` to `weekly-measurement-review.py`'s top-level except (copy the sibling pattern). Re-copy to `~/.local/bin/`.
3. **[#3 Rel]** Make `fetch_paginated` raise on a mid-pagination error instead of `break`-ing (match rollout-check). Re-copy to `~/.local/bin/`.
4. **[#6 UX]** If unintended, make wishlist "Add from Photo" `btn-primary` to match collection.

## Fixes applied 2026-06-29 (this session — all 7 top findings + quick wins)

Verified: 1236 unit · 118 mocked E2E (incl. wishlist add-from-photo) · 54 send-broadcast deno tests · smoke 5/5. RPC correctness checked against raw aggregates; index use confirmed via EXPLAIN. SW cache v866→v867.

| # | Item | Fix | Verify |
|---|------|-----|--------|
| 4/5 | search_path regression | `ALTER FUNCTION` re-pinned both admin RPCs; **source migration files updated with inline `SET search_path`** so a future `CREATE OR REPLACE` can't drop it again. | Live `proconfig: search_path=pg_catalog, public` on both. **DEPLOYED.** |
| 1 | weekly-review no failure alert | Added `notify_failure(tb)` (mirrors rollout-check/nightly) to the `__main__` guard. | py_compile OK; copied to `~/.local/bin/wrotate-weekly-review.py`. |
| 3 | weekly-review silent truncation | `fetch_paginated` now `raise`s on a mid-pagination PostgREST error instead of returning partial rows. | py_compile OK; deployed copy refreshed. |
| 2 | admin modal ~1.1s tick_logs scan | Partial index `idx_ttl_summary_user` on `(messages::jsonb->>'user_id')::uuid` WHERE session_summary (1170 rows, write-light); folded measurement aggregation into `admin_user_detail`; **dropped the client's 960ms double-LIKE query** + parse loop. | EXPLAIN → `Index Scan using idx_ttl_summary_user`. RPC output matches raw aggregate (masont 85/27/5/noisy). **DEPLOYED.** |
| 7 | send-broadcast unpaginated reads | Added `fetchAllRows` page-loop; applied to the cohort `profiles` read + 3 exclusion-list reads (campaign_sends, timegrapher_results, email_events). | deno check clean; 54 lib tests; **DEPLOYED** + smoke 5/5. |
| 6 | Add-from-Photo CTA hierarchy | Wishlist empty-state "Add from Photo" → `btn-primary`, "Add to Wishlist" → `btn-ghost` (matches Collection; Photo is the promoted lead path). | mocked E2E green. Frontend — ships on push. |

Root-cause note for #4/#5: the `CREATE OR REPLACE`-drops-config pattern bit twice. Mitigation: all future SECURITY DEFINER redefinitions must carry `SET search_path` inline (now true for the admin-RPC source files; featured-post RPCs already did).

## By category (counts)
- **Security:** 0 Crit · 0 High · 2 Med (both NEW regressions) · 2 Low (carried). New surfaces clean.
- **Reliability:** 1 High (NEW) · 1 Med NEW + 1 Med carried · 4 Low. 5 of 6 new surfaces clean.
- **Usability:** 0 High · 1 Med (NEW, possibly intentional) · 2 Low. 3 prior items confirmed FIXED.
- **Performance:** 1 High (carried, compounded) · 2 Med carried · 2 Low. Tick-log table shrank 3×.
